# Status App Configuration
# Copy this file to .env and customize as needed

# Owner handle for the default status page
OWNER_HANDLE=zzstoatzz.io

# Database URL (defaults to local SQLite)
# For production, consider using a persistent volume
DATABASE_URL=sqlite://./statusphere.sqlite3

# OAuth redirect base URL (must match your deployment URL)
# For production: https://status.yourdomain.com
OAUTH_REDIRECT_BASE=http://localhost:8080

# Server configuration
SERVER_HOST=127.0.0.1
SERVER_PORT=8080

# Enable firehose ingester (true/false)
# Set to true in production to receive real-time updates
ENABLE_FIREHOSE=false

# Log level (trace, debug, info, warn, error)
RUST_LOG=info

# Note: Admin DID is intentionally hardcoded in the code for security
# This prevents accidental exposure or modification