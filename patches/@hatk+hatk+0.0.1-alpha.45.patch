diff --git a/node_modules/@hatk/hatk/dist/config.d.ts b/node_modules/@hatk/hatk/dist/config.d.ts
index d78f161..074eb95 100644
--- a/node_modules/@hatk/hatk/dist/config.d.ts
+++ b/node_modules/@hatk/hatk/dist/config.d.ts
@@ -32,6 +32,8 @@ export interface BackfillConfig {
 }
 export interface HatkConfig {
     relay: string;
+    /** Optional Jetstream endpoint for filtered ingest (e.g. wss://stream.waow.tech/subscribe). */
+    jetstream?: string;
     plc: string;
     port: number;
     databaseEngine: 'duckdb' | 'sqlite';
diff --git a/node_modules/@hatk/hatk/dist/config.js b/node_modules/@hatk/hatk/dist/config.js
index 50162fa..c2cbf55 100644
--- a/node_modules/@hatk/hatk/dist/config.js
+++ b/node_modules/@hatk/hatk/dist/config.js
@@ -40,6 +40,9 @@ export async function loadConfig(configPath) {
     const database = env.DATABASE || parsed.database;
     const config = {
         relay: env.RELAY || parsed.relay || 'ws://localhost:2583',
+        // Optional filtered ingest. When set, the indexer subscribes here with
+        // wantedCollections instead of tailing the whole relay firehose.
+        jetstream: env.JETSTREAM || parsed.jetstream || undefined,
         plc: env.DID_PLC_URL || parsed.plc || 'https://plc.directory',
         port: parseInt(env.PORT || '') || parsed.port || 3000,
         databaseEngine: (env.DATABASE_ENGINE || parsed.databaseEngine || 'sqlite'),
diff --git a/node_modules/@hatk/hatk/dist/database/db.js b/node_modules/@hatk/hatk/dist/database/db.js
index 0d6b89c..d2a8ecf 100644
--- a/node_modules/@hatk/hatk/dist/database/db.js
+++ b/node_modules/@hatk/hatk/dist/database/db.js
@@ -38,6 +38,7 @@ export async function runBatch(operations) {
 async function all(sql, params = []) {
     return port.query(sql, params);
 }
+let deferredDdl = [];
 export async function initDatabase(adapter, dbPath, tableSchemas, ddlStatements) {
     port = adapter;
     dialect = getDialect(adapter.dialect);
@@ -45,8 +46,18 @@ export async function initDatabase(adapter, dbPath, tableSchemas, ddlStatements)
     for (const schema of tableSchemas) {
         schemas.set(schema.collection, schema);
     }
+    // A lexicon that gained a field emits DDL indexing its column (a strongRef's
+    // {name}_uri, say) — but that column is only added later, by migrateSchema.
+    // Defer whatever fails here and replay it once the migration has run, rather
+    // than crashing on startup against an existing database.
+    deferredDdl = [];
     for (const ddl of ddlStatements) {
-        await port.executeMultiple(ddl);
+        try {
+            await port.executeMultiple(ddl);
+        }
+        catch {
+            deferredDdl.push(ddl);
+        }
     }
     // Internal tables for backfill state
     await run(`CREATE TABLE IF NOT EXISTS _repos (
@@ -268,6 +279,15 @@ export async function migrateSchema(tableSchemas) {
     if (changes.length > 0) {
         await applyMigrationChanges(changes);
     }
+    // Replay DDL that could not run before the new columns existed. Anything
+    // still failing now is a real error and is allowed to throw.
+    if (deferredDdl.length > 0) {
+        const pending = deferredDdl;
+        deferredDdl = [];
+        for (const ddl of pending) {
+            await port.executeMultiple(ddl);
+        }
+    }
     // Check for empty collection tables — these are newly added and need backfill
     // Skip on fresh DB (no repos yet) since backfill runs naturally
     const [hasRepos] = await all(`SELECT 1 FROM _repos LIMIT 1`);
diff --git a/node_modules/@hatk/hatk/dist/database/fts.js b/node_modules/@hatk/hatk/dist/database/fts.js
index 037715c..d59f7da 100644
--- a/node_modules/@hatk/hatk/dist/database/fts.js
+++ b/node_modules/@hatk/hatk/dist/database/fts.js
@@ -158,6 +158,26 @@ function computeFtsSchema(collection) {
     ftsSchemaCache.set(collection, result);
     return result;
 }
+/**
+ * Does the existing shadow table still carry every search column?
+ * Returns true when it cannot be determined, so an introspection failure
+ * degrades to the previous behaviour rather than rebuilding on every boot.
+ */
+async function ftsColumnsMatch(shadowTable, searchColNames) {
+    try {
+        const dialect = getSqlDialect();
+        const rows = await querySQL(dialect.introspectColumnsQuery(shadowTable), []);
+        if (!rows || rows.length === 0)
+            return true;
+        const present = new Set(rows.map((r) => r.name ?? r.column_name).filter(Boolean));
+        if (present.size === 0)
+            return true;
+        return searchColNames.every((c) => present.has(c));
+    }
+    catch {
+        return true;
+    }
+}
 /**
  * Build FTS index for a collection.
  * Creates a shadow table copy and indexes all TEXT NOT NULL columns
@@ -169,14 +189,20 @@ export async function buildFtsIndex(collection) {
     const { searchColNames, sourceQuery, safeName } = computeFtsSchema(collection);
     if (searchColNames.length === 0)
         return;
-    // For incremental ports: skip rebuild if index already exists
+    // For incremental ports: skip rebuild if the index already exists AND still
+    // matches the collection's search columns. Skipping unconditionally meant a
+    // lexicon that gained a field kept a shadow table without that column, and
+    // every subsequent updateFtsRecord failed with "no such column: <field>".
     if (searchPort.indexExists) {
         const exists = await searchPort.indexExists(safeName);
-        if (exists) {
+        if (exists && (await ftsColumnsMatch(safeName, searchColNames))) {
             searchColumnCache.set(collection, searchColNames);
             lastRebuiltAt.set(collection, new Date().toISOString());
             return;
         }
+        if (exists) {
+            emit('fts', 'schema_drift', { collection, table: safeName });
+        }
     }
     await searchPort.buildIndex(safeName, sourceQuery, searchColNames);
     searchColumnCache.set(collection, searchColNames);
diff --git a/node_modules/@hatk/hatk/dist/indexer.js b/node_modules/@hatk/hatk/dist/indexer.js
index 04df7a0..39dbc8b 100644
--- a/node_modules/@hatk/hatk/dist/indexer.js
+++ b/node_modules/@hatk/hatk/dist/indexer.js
@@ -22,6 +22,14 @@ const backfillPromises = new Map();
 const pendingReschedule = new Set();
 // In-memory cache of repo status to avoid flooding the DB read queue
 const repoStatusCache = new Map();
+// Cursor save guard — ensures the periodic cursor saver is started exactly
+// once across all `startIndexer` invocations (the function is re-entered on
+// every reconnect).
+let cursorSaverStarted = false;
+/** Which _cursor row this indexer owns — 'relay' (firehose seq) or 'jetstream' (time_us). */
+let cursorKey = 'relay';
+/** How often to persist `lastSeq` to `_cursor.relay` independent of buffer flushes. */
+const CURSOR_SAVE_INTERVAL_MS = 10_000;
 // Set by startIndexer
 let indexerCollections;
 let indexerSignalCollections;
@@ -55,7 +63,7 @@ async function flushBuffer() {
     }
     if (lastSeq !== null) {
         try {
-            await setCursor('relay', String(lastSeq));
+            await setCursor(cursorKey, String(lastSeq));
         }
         catch (err) {
             cursorError = err.message;
@@ -231,6 +239,31 @@ function startMemoryDiagnostics() {
         });
     }, 30_000);
 }
+/**
+ * Periodically persist `lastSeq` to `_cursor.relay` so the indexer can resume
+ * near live on restart even when no records have been flushed recently.
+ *
+ * `flushBuffer` saves the cursor as a side effect of inserting records — fine
+ * for high-volume collections, but for deployments that index rare/low-volume
+ * collections (custom lexicons, niche feeds) the buffer may not flush for
+ * hours or days. Without a heartbeat, each restart resumes from the last
+ * flush, which can be arbitrarily stale; on reconnect the relay then replays
+ * a huge backlog of irrelevant events before the indexer reaches live and
+ * starts processing fresh commits.
+ *
+ * Idempotent: guarded by {@link cursorSaverStarted} so reconnect loops don't
+ * stack multiple timers.
+ */
+function startCursorSaver() {
+    if (cursorSaverStarted)
+        return;
+    cursorSaverStarted = true;
+    setInterval(() => {
+        if (lastSeq !== null) {
+            setCursor(cursorKey, String(lastSeq)).catch(() => { });
+        }
+    }, CURSOR_SAVE_INTERVAL_MS);
+}
 /**
  * Connect to the AT Protocol relay firehose and begin indexing.
  *
@@ -251,6 +284,7 @@ export async function startIndexer(opts) {
     indexerFetchTimeout = fetchTimeout;
     indexerMaxRetries = opts.maxRetries;
     maxConcurrentBackfills = opts.parallelism ?? 3;
+    startCursorSaver();
     // Pre-populate repo status cache from DB so non-signal updates
     // (e.g. profile changes) are processed for already-tracked DIDs
     if (repoStatusCache.size === 0) {
@@ -261,6 +295,10 @@ export async function startIndexer(opts) {
         log(`[indexer] Warmed repo status cache with ${statuses.length} entries`);
     }
     // startMemoryDiagnostics()
+    if (opts.jetstreamUrl) {
+        return startJetstreamIndexer(opts);
+    }
+    cursorKey = 'relay';
     let wsUrl = `${relayUrl}/xrpc/com.atproto.sync.subscribeRepos`;
     if (cursor) {
         wsUrl += `?cursor=${cursor}`;
@@ -285,10 +323,140 @@ export async function startIndexer(opts) {
     ws.addEventListener('open', () => log('[indexer] Connected to relay'));
     ws.addEventListener('close', () => {
         log('[indexer] Disconnected, reconnecting in 3s...');
-        setTimeout(() => startIndexer(opts), 3000);
+        // Resume from where we actually got to, not from where this process
+        // started. Relays drop the connection every few minutes; reusing the
+        // original opts.cursor rewound that far back on every reconnect, so an
+        // indexer behind by more than one disconnect interval could never catch
+        // up — it replayed the same window forever.
+        setTimeout(() => startIndexer({ ...opts, cursor: lastSeq ?? opts.cursor }), 3000);
     });
     return ws;
 }
+
+/**
+ * Filtered ingest via a Jetstream endpoint.
+ *
+ * `subscribeRepos` delivers every commit on the network — for an app indexing a
+ * rare collection that means chewing through thousands of irrelevant events per
+ * second to find one that matters, and any downtime takes many times its own
+ * duration to recover. Jetstream applies `wantedCollections` server-side, so the
+ * socket carries only what this app indexes and the indexer cannot fall behind.
+ *
+ * Cursors here are microsecond timestamps (`time_us`), not sequence numbers.
+ */
+async function startJetstreamIndexer(opts) {
+    const { jetstreamUrl, collections, cursor } = opts;
+    cursorKey = 'jetstream';
+    const params = new URLSearchParams();
+    // Jetstream caps the post-dedupe collection list at 100.
+    for (const c of [...collections].slice(0, 100)) {
+        params.append('wantedCollections', c);
+    }
+    if (cursor) {
+        params.set('cursor', String(cursor));
+        log(`[indexer] Resuming from jetstream cursor ${cursor}`);
+    }
+    const wsUrl = `${jetstreamUrl}?${params.toString()}`;
+    log(`[indexer] Connecting to ${jetstreamUrl} (filtered: ${[...collections].join(', ')})...`);
+    const ws = new WebSocket(wsUrl);
+    ws.addEventListener('message', (event) => {
+        try {
+            if (typeof event.data !== 'string')
+                return;
+            processJetstreamEvent(JSON.parse(event.data), collections);
+        }
+        catch {
+            // Skip unparseable events silently, as the firehose path does
+        }
+    });
+    ws.addEventListener('open', () => log('[indexer] Connected to jetstream'));
+    ws.addEventListener('close', () => {
+        log('[indexer] Disconnected, reconnecting in 3s...');
+        // Resume from where we actually got to, not from where this process
+        // started. Relays drop the connection every few minutes; reusing the
+        // original opts.cursor rewound that far back on every reconnect, so an
+        // indexer behind by more than one disconnect interval could never catch
+        // up — it replayed the same window forever.
+        setTimeout(() => startIndexer({ ...opts, cursor: lastSeq ?? opts.cursor }), 3000);
+    });
+    return ws;
+}
+/**
+ * Handle one Jetstream event, mirroring the decisions {@link processMessage}
+ * makes for the firehose: identity updates, auto-backfill on signal
+ * collections, lexicon validation, and buffered writes.
+ */
+function processJetstreamEvent(evt, collections) {
+    if (evt.time_us)
+        lastSeq = evt.time_us;
+    const did = evt.did;
+    if (!did)
+        return;
+    if (indexerPinnedRepos && !indexerPinnedRepos.has(did))
+        return;
+    if (evt.kind === 'identity') {
+        const handle = evt.identity?.handle;
+        if (handle && repoStatusCache.has(did)) {
+            updateRepoHandle(did, handle).catch(() => { });
+        }
+        return;
+    }
+    if (evt.kind !== 'commit' || !evt.commit)
+        return;
+    const op = evt.commit;
+    const collection = op.collection;
+    // identity/account events bypass wantedCollections, and a server could widen
+    // the filter; never trust it as the only gate.
+    if (!collection || !collections.has(collection))
+        return;
+    const uri = `at://${did}/${collection}/${op.rkey}`;
+    const isSignal = indexerSignalCollections.has(collection);
+    const cachedStatus = repoStatusCache.get(did);
+    const repoStatus = cachedStatus === undefined || cachedStatus === 'unknown' ? null : cachedStatus;
+    if (cachedStatus === undefined) {
+        repoStatusCache.set(did, 'unknown');
+    }
+    if (isSignal) {
+        if (repoStatus === null && backfillInFlight.size < maxConcurrentBackfills) {
+            repoStatusCache.set(did, 'pending');
+            triggerAutoBackfill(did);
+        }
+        else if (repoStatus === null) {
+            repoStatusCache.set(did, 'pending');
+            setRepoStatus(did, 'pending');
+        }
+    }
+    else if (repoStatus === null) {
+        // non-signal collection from a DID we do not track yet
+        return;
+    }
+    if (op.operation === 'delete') {
+        deleteRecord(collection, uri);
+        return;
+    }
+    const record = op.record;
+    if (!record || !op.cid)
+        return;
+    if (record.$type && record.$type !== collection)
+        return;
+    const validationError = validateRecord(getLexiconArray(), collection, record);
+    if (validationError) {
+        emit('indexer', 'validation_skip', {
+            uri,
+            collection,
+            path: validationError.path,
+            error: validationError.message,
+        });
+        return;
+    }
+    const item = { collection, uri, cid: op.cid, authorDid: did, record };
+    if (pendingBuffers.has(did)) {
+        pendingBuffers.get(did).push(item);
+    }
+    else {
+        bufferWrite(item);
+    }
+}
 /**
  * Process a single firehose message. Decodes the CBOR header/body, filters
  * for relevant collections, validates records against lexicons, and routes
@@ -357,32 +525,35 @@ function processMessage(bytes, collections) {
             deleteRecord(collection, uri);
             continue;
         }
-        for (const [cid, data] of blocks) {
-            try {
-                const { value: record } = cborDecode(data);
-                if (record?.$type === collection) {
-                    const validationError = validateRecord(getLexiconArray(), collection, record);
-                    if (validationError) {
-                        emit('indexer', 'validation_skip', {
-                            uri,
-                            collection,
-                            path: validationError.path,
-                            error: validationError.message,
-                        });
-                        break;
-                    }
-                    const item = { collection, uri, cid, authorDid: did, record };
-                    // If DID is mid-backfill, buffer instead of writing directly
-                    if (pendingBuffers.has(did)) {
-                        pendingBuffers.get(did).push(item);
-                    }
-                    else {
-                        bufferWrite(item);
-                    }
-                    break;
+        const opCid = typeof op.cid === 'string' ? op.cid : op.cid?.$link;
+        if (!opCid)
+            continue;
+        const data = blocks.get(opCid);
+        if (!data)
+            continue;
+        try {
+            const { value: record } = cborDecode(data);
+            if (record?.$type === collection) {
+                const validationError = validateRecord(getLexiconArray(), collection, record);
+                if (validationError) {
+                    emit('indexer', 'validation_skip', {
+                        uri,
+                        collection,
+                        path: validationError.path,
+                        error: validationError.message,
+                    });
+                    continue;
+                }
+                const item = { collection, uri, cid: opCid, authorDid: did, record };
+                // If DID is mid-backfill, buffer instead of writing directly
+                if (pendingBuffers.has(did)) {
+                    pendingBuffers.get(did).push(item);
+                }
+                else {
+                    bufferWrite(item);
                 }
             }
-            catch { }
         }
+        catch { }
     }
 }
diff --git a/node_modules/@hatk/hatk/dist/main.js b/node_modules/@hatk/hatk/dist/main.js
index 8559dc3..0b5cea8 100644
--- a/node_modules/@hatk/hatk/dist/main.js
+++ b/node_modules/@hatk/hatk/dist/main.js
@@ -186,9 +186,13 @@ log(`  Feeds: ${listFeeds()
     .map((f) => f.name)
     .join(', ')}`);
 // 6. Start indexer with cursor
-const cursor = await getCursor('relay');
+// Cursors are not interchangeable: the firehose counts sequence numbers, a
+// Jetstream cursor is a microsecond timestamp. Keep them under separate keys so
+// switching ingest modes cannot resume from a nonsensical position.
+const cursor = await getCursor(config.jetstream ? 'jetstream' : 'relay');
 startIndexer({
     relayUrl: config.relay,
+    jetstreamUrl: config.jetstream,
     collections: collectionSet,
     signalCollections: config.backfill.signalCollections ? new Set(config.backfill.signalCollections) : undefined,
     pinnedRepos: config.backfill.repos ? new Set(config.backfill.repos) : undefined,
